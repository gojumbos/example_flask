Commands:

flask --app app run

curl "http://127.0.0.1:5000"

curl -X POST -H "Content-Type: application/json" -d '{"name":"John", "age":22}' http://127.0.0.1:5000/post_example


CONFIG:
git config user.name "<username>"
git config user.email "<email>"


INIT:
git init
git add .
git commit -m "First commit"
git branch -M main
git remote add origin <repo url>
git push -u origin main